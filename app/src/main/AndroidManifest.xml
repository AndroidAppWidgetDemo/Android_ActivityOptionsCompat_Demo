<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="demo.xiaxl.com.activity_options">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="demo.xiaxl.com.activity_options.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="demo.xiaxl.com.activity_options.BActivity"
            android:theme="@style/TransTheme"></activity>

        <activity
            android:name="demo.xiaxl.com.activity_options.CActivity"
            android:theme="@style/TransTheme"></activity>
    </application>

</manifest>